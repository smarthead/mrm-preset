const config = {
    ts: {
        rules: {
            curly: [2, 'all'],
        },
    },
    reactTs: {
        rules: {
            'react/prop-types': 0,
        }
    },
};

module.exports = config;
